__Personal info__

```json
{
  "Name": "MD Mahmuduzzaman",
  "Short name": "Niloy",
  "Birthday": "11 November",
  "From": "Chuadanga Sadar"
}
```

__Code__

```c
// 1
#include <Servo.h>
#include <math.h>

// 1
Servo dancer;
double x = 0.0;
int servo_pin = 9;
double step = 0.005;

void setup() 
{
  dancer.attach(servo_pin);
}

void loop() 
{
  double y = pow(sin(x), 2);

  // 0
  float servo_angle = y*180.0;
  dancer.write(servo_angle);
  delay(10);
  x += step;

  if (x > 2*PI) x = 0;
}
```